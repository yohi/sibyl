name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: "1.3.14"
      - run: bun install --frozen-lockfile
      - run: bun run lint
      - run: bun run typecheck
      - run: bun run build
      - run: bun run test

  node-runtime:
    timeout-minutes: 15
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: "1.3.14"
      - run: bun install --frozen-lockfile
      - run: bun run build
      - run: >-
          node --input-type=module -e
          'import("./dist/index.js").then(async ({ PtyManager }) => { const manager = new PtyManager(); const terminal = await manager.spawn({ command: "sh", args: ["-c", "exit 0"] }); await manager.terminate(terminal.id); })'

  container-runtime:
    timeout-minutes: 15
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: >-
          docker run --rm
          -v "$GITHUB_WORKSPACE:/workspace"
          -w /workspace
          oven/bun:1.3.14
          sh -c 'apt-get update -qq && apt-get install -y -qq --no-install-recommends python3 make g++ procps && bun install --frozen-lockfile && bun run lint && bun run typecheck && bun run build && bun run test'
